services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DEBUG: "false"
      DATABASE_PATH: /data/db.sqlite3
    volumes:
      - app_data:/data
      - app_media:/app/media
    restart: unless-stopped

volumes:
  app_data:
  app_media:
